# AnoGAN-Replication
AnoGAN-Replication implements an unsupervised GAN framework for anomaly detection in medical imaging. It trains a generative model on healthy data to identify and score anomalous regions in new images.
